# 潔美淨清潔社 — 廣告追蹤上線設定說明

> 這份是給老闆看的白話操作說明。照著做，就能讓網站開始記錄「有多少人打電話、加 LINE、送出預約」，廣告才知道錢花得有沒有效。
>
> **預計時間：約 30～40 分鐘。用電腦操作比手機順。**

---

## 一、你最後要拿到的東西

整個設定的目標，就是拿到下面這 **2 個編號**（最重要），填進網站的設定檔：

| 編號名稱 | 長相 | 從哪裡來 |
|---|---|---|
| **GA4 評估 ID** | `G-` 開頭，例如 `G-ABCD12345` | Google Analytics |
| **Ads 轉換 ID** | `AW-` 開頭，例如 `AW-1234567890` | Google Ads |

> 還有 3 個「轉換標籤」是進階用的（電話、LINE、表單各一個），**第一次上線可以先不用管**，留著預設值網站也能正常運作。詳見最後的〈附錄〉。

**最省事的做法：** 你只要照下面把帳號開好、把畫面上的編號截圖或複製下來，**直接貼給我（Claude），我幫你填進設定檔、檢查無誤。** 不用自己改程式。

---

## 二、STEP 1：開 Google Analytics（拿 `G-` 開頭那組）

Google Analytics（簡稱 GA4）是免費的，用來看「有多少人來網站、做了什麼」。

1. 用電腦開 **https://analytics.google.com**，用你的 Google 帳號登入。
2. 第一次會請你「開始測量」。點 **「開始測量 / 建立」**。
3. **建立帳戶**：帳戶名稱填「潔美淨清潔社」→ 下一步。
4. **建立資源**：
   - 資源名稱填「潔美淨官網」
   - 時區選 **台灣**、貨幣選 **新台幣 TWD**
   - 下一步，產業類別選「商業與工業服務」之類即可，規模選最小。
5. 選擇平台時點 **「網站」**：
   - 網站網址填 `https://0988145875.com.tw`
   - 串流名稱填「潔美淨官網」→ **建立串流**。
6. 建立完成後，畫面會出現 **「評估 ID」**，長得像 **`G-XXXXXXXXXX`**。
   👉 **把這串複製/截圖下來，這就是第 1 個編號。**

---

## 三、STEP 2：開 Google Ads（拿 `AW-` 開頭那組）

Google Ads 就是之後要下廣告的地方。

1. 用電腦開 **https://ads.google.com**，用同一個 Google 帳號登入。
2. 跟著指示建立帳戶（填公司名、國家台灣、幣別 TWD、時區台灣）。
   - 如果它一直逼你「設定第一個廣告」，可以找 **「切換到專家模式」** 或先隨意建立、之後再暫停，重點是先把帳號開出來。
3. 帳號開好後，左上角找 **「工具」**（板手圖示）→ 找到 **「轉換」**（在「評估 / Measurement」分類下）。
4. 點 **「+ 新增轉換動作」** → 選 **「網站」**。
5. 網站網址填 `0988145875.com.tw`，照指示往下。
6. 設定到「安裝代碼」那一步時，選 **「自行安裝 Google 代碼」**，畫面會出現一段程式碼，裡面有一串 **`AW-XXXXXXXXXX`**。
   👉 **把這串 `AW-` 複製/截圖下來，這就是第 2 個編號。**

> 看到 `send_to: 'AW-XXXXXXXXXX/abcDEF123'` 這種「AW-斜線-一串字」的，也一起截圖——那是進階用的轉換標籤，貼給我會更完整。

---

## 四、STEP 3：讓 Google Ads 收得到「轉換」（重要）

光有編號還不夠，要告訴 Ads「什麼動作算成功」。最簡單的方式是用 GA4 串接：

1. 回到 **Google Analytics**（STEP 1 那裡）。
2. 左下角 **「管理」**（齒輪）→ 找 **「事件 / Events」**。
   - 廣告上線、有人點電話或 LINE 後，這裡會自動出現 `phone_click`、`line_click`、`form_submit` 這幾個事件（這是網站已經幫你埋好的）。
   - 把這三個事件各自旁邊的開關打開，標記為 **「關鍵事件 / Key event」**。
3. 一樣在「管理」裡找 **「Google Ads 連結」** → **連結** → 選你剛開的 Ads 帳號 → 完成。
4. 連結後，回到 **Google Ads → 工具 → 轉換 → 匯入**，把 GA4 的那三個關鍵事件匯入成轉換即可。

> 這步如果卡住，不用緊張，**先把帳號開好、編號拿到就好**，這部分我可以遠端指導你，或你截圖給我看畫面，我一步一步帶。

---

## 五、STEP 4：把編號填進網站設定檔

網站裡有一個檔案叫 **`analytics.js`**，最上面長這樣：

```js
var CFG = {
    GA4_ID:     'G-XXXXXXXXXX',
    ADS_ID:     'AW-XXXXXXXXXX',
    CONV_PHONE: 'AW-XXXXXXXXXX/PhoneLabel',
    CONV_LINE:  'AW-XXXXXXXXXX/LineLabel',
    CONV_FORM:  'AW-XXXXXXXXXX/FormLabel'
  };
```

把 `G-XXXXXXXXXX` 換成你的 GA4 編號、`AW-XXXXXXXXXX` 換成你的 Ads 編號就好。例如：

```js
var CFG = {
    GA4_ID:     'G-ABCD12345',
    ADS_ID:     'AW-1234567890',
    CONV_PHONE: 'AW-XXXXXXXXXX/PhoneLabel',   ← 進階，先不動沒關係
    CONV_LINE:  'AW-XXXXXXXXXX/LineLabel',    ← 進階，先不動沒關係
    CONV_FORM:  'AW-XXXXXXXXXX/FormLabel'     ← 進階，先不動沒關係
  };
```

**最簡單：把你拿到的編號貼給我（Claude），我直接幫你填好、檢查、推上去。** 你不必碰程式碼。

---

## 六、STEP 5：確認有沒有生效

填好、網站更新後：

1. 用手機開自己的網站 `0988145875.com.tw`，隨便點一下電話或 LINE 按鈕。
2. 回到 **Google Analytics → 報表 → 即時 / Realtime**。
3. 如果看到「目前有 1 位使用者」以及剛剛的事件，就代表 **追蹤成功了** ✅。

---

## ⚠️ 七、最後一件事：讓網站正式更新

目前這五輪的所有優化（新 logo、真人照片、新竹頁、追蹤碼…）都在一個**開發分支**上，還沒有覆蓋到正式對外的網站。

等你確認都沒問題，要把這些改動**正式發佈到 `0988145875.com.tw`**，需要做一次「合併上線」。這一步我可以幫你處理 —— 你跟我說「可以上線了」，我再協助合併（或開 PR 給你確認）。

---

## 附錄：進階 — 自建 3 個 Ads 轉換標籤

如果你（或代操廣告的人）想要更精準分開計算「電話 / LINE / 表單」三種轉換，可以在 Google Ads 各建一個轉換動作，每個會給一串 `AW-XXXXXXXXXX/一串標籤`，分別填進：

| 設定檔欄位 | 對應動作 | 網站送出的事件名 |
|---|---|---|
| `CONV_PHONE` | 電話點擊 | `phone_click` |
| `CONV_LINE` | LINE 點擊 | `line_click` |
| `CONV_FORM` | 預約表單送出 | `form_submit` |
| `CONV_CALCULATOR` | 費用試算完成 | `calculator_used` |

填了這些轉換標籤，網站除了記錄到 GA4，也會直接回報給 Google Ads 當轉換。沒填（維持 `XXXX`）也完全沒問題，網站會自動略過、不會出錯。

另外目前網站也會記錄：

- `booking_cta_click`：點擊站內「線上預約 / 填寫需求」按鈕
- `booking_form_submit`：送出預約需求並開啟 LINE
- `estimate_booking_click`：費用試算後點擊「傳送估算結果給師傅」
- `map_click`：點擊 Google 地圖

Google Ads 上線前，也可以參考 `Google廣告投放準備清單.md`，裡面有活動架構、關鍵字、否定關鍵字與廣告文案範例。

---

## 常見問題

**Q：一定要會寫程式嗎？**
不用。你只要把帳號開好、編號截圖貼給我，剩下我處理。

**Q：開帳號要錢嗎？**
GA4 完全免費。Google Ads 開帳號免費，只有實際投放廣告才會扣廣告費。

**Q：編號填錯會怎樣？**
不會弄壞網站。網站設計成「編號沒填對就自動不啟用追蹤」，照常運作。

**Q：5 個編號我只拿到 2 個（G- 和 AW-）可以嗎？**
可以，這 2 個是核心，先上線完全足夠。另外 3 個是進階選項。
